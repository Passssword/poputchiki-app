
let initialState = {
    UserData: {
        UserID: null,
        Login: null,
        isAuth: false
    }
}

const reducerAuth = (state = initialState, action) => {
    
    let stateCopy = {...state}

}

export default reducerAuth;